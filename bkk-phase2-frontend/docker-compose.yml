version: '3.7'

services:

  bkk-phase2:
    container_name: frontend
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - '.:/app'
      - '/app/node_modules'
    ports:
      - '8088:8088'
    environment:
      - CHOKIDAR_USEPOLLING=true